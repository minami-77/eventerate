<div class="container">

  <div class="title d-flex justify-content-between mt-4">
    <div>
      <h1>Event Title : <%=@event.title%></h1>
      <p><%= @event.organization.name %></p>
    </div>
    <div class="p-3">
      <%= link_to edit_event_path(@event) do %>
        <i class="fa-solid fa-pen-to-square edit-icon"></i>
      <% end %>
    </div>
  </div>

<%# Accordion-Overview %>
<div class="accordion" id="accordionPanelsStayOpenExample">
  <div class="accordion-item">
    <h2 class="accordion-header" id="panelsStayOpen-headingOverview">
      <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseOverview" aria-expanded="true" aria-controls="panelsStayOpen-collapseOne">
        <strong>Overview</strong>
      </button>
    </h2>
    <div id="panelsStayOpen-collapseOverview" class="accordion-collapse collapse show" aria-labelledby="panelsStayOpen-headingOverview">
      <div class="accordion-body">
        <div class="row">
          <div class="col-4">
            <p><strong>Date: </strong><%= @event.date %></p>
            <p><strong>Duration: </strong><%= @event.duration %></p>
          </div>
          <div class="col-8">
            <p><strong>Activities:</strong></p>
            <ol>
              <% @event.activities.each do |activity| %>
                <li>
                  <%= activity.title.capitalize %>
                </li>
              <% end %>
            </ol>
          </div>
        </div>
      </div>
    </div>
  </div>

<%# Accordion-Activities %>
  <% if  @event.activities_events.any?  %>
    <% @event.activities_events.each_with_index do |activity, index| %>
      <div class="accordion-item">
        <h2 class="accordion-header" id="panelsStayOpen-heading<%= index %>">
          <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapse<%= index %>" aria-expanded="false" aria-controls="panelsStayOpen-collapse<%= index %>">
            <strong>Activity <%= index + 1 %>: <%= activity.display_title.capitalize %></strong>
          </button>
        </h2>
        <div id="panelsStayOpen-collapse<%= index %>" class="accordion-collapse collapse" aria-labelledby="panelsStayOpen-heading<%= index %>">
          <div class="accordion-body">
            <% description_summary = activity.display_description.split("\n").first.gsub(/\*\*Description\*\*[:\s]*/, '').strip %>
<p><strong>Description:</strong> <%= description_summary %></p>
          <%# Step-by-Step Instructions %>
              <% if activity.display_description.include?("**Step-by-Step Instructions**") %>
                <h5>Step-by-Step Instructions:</h5>
                <ul>
                  <% activity.display_description.scan(/\d+\.\s(.+)/).each do |step| %>
                    <li><%= step.first %></li>
                  <% end %>
                </ul>
              <% end %>

              <%# Materials %>
              <% if activity.display_description.include?("**Materials**") %>
                <h5>Materials:</h5>
                <ul>
                  <% activity.display_description.scan(/\*\*Materials\*\*: (.+)/).each do |material| %>
                    <% material.first.split(', ').each do |item| %>
                      <li><%= item.strip %></li>
                    <% end %>
                  <% end %>
                </ul>
              <% end %>
              <%= link_to edit_activity_path(event_id: @event, id: activity), class: "btn btn-gradient-oval" do %>
                <i class="fa-solid fa-pen-to-square"></i>
                Edit Activity
              <% end %>
              <%= link_to delete_activity_path(event_id: @event, id: activity), class: "btn btn-gradient-oval", data: { turbo_method: :delete } do %>
                <i class="fa-solid fa-trash-can"></i>
                Delete Activity
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
<%# Accordion-Tasks %>
    <div class="accordion-item">
    <h2 class="accordion-header" id="panelsStayOpen-headingSeven">
      <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseSeven" aria-expanded="false" aria-controls="panelsStayOpen-collapseSeven">
        <strong>Tasks</strong>
      </button>
    </h2>
    <div id="panelsStayOpen-collapseSeven" class="accordion-collapse collapse" aria-labelledby="panelsStayOpen-headingSeven">
      <div class="accordion-body">

      <%# create a new task %>
      <button type="button" class="btn btn-gradient-oval" data-action="click->assign-tasks#openModal" data-bs-toggle="modal" data-bs-target="#taskModal" data-bs-whatever="@mdo">Add a task</button>
      <%= render "create_tasks", event: @event, task: @task %>

      <%# task container %>
      <% if @event.tasks.any?  %>
        <div class="list-container">
          <ul class="list-group list-group-flush">
            <% @event.tasks.where.not(title: nil).order(created_at: :desc).each do |task| %>
              <li class="list-group-item">
                <div class="row">
                  <div class="col-6">
                    <p><strong><%=task.title.capitalize%></strong></p>
                    <p>Assigned to:
                      <% user_id = task.tasks_users.first.user_id %>
                      <% user = User.find_by_id(user_id) %>
                      <%= user.first_name + ' ' + user.last_name %>
                    </p>
                    <p>Status: <%= task.completed ? "Completed" : "Not Completed" %></p>
                    <p>Comment: <%= task.comment%></p>
                  </div>

      <%# task assign form by stimulus %>
                  <div class="col-6">
                    <div data-controller="assign-tasks" >
                      <button class="btn btn-gradient-oval" data-action="click->assign-tasks#assign">
                      Edit
                      </button>
                      <div class="d-none" data-assign-tasks-target = "form">
                        <% organization_users = @users.select { |user| user.organizations.include?(@event.organization)}%>
                        <%= simple_form_for task, url: event_task_path(@event, task), method: :patch do |f| %>
                        <%= f.input :user_id,
                          collection: organization_users.map { |user| ["#{user.first_name} #{user.last_name}", user.id] } %>
                        <%= f.input :completed, collection: ["Completed", "Not completed"]%>
                        <%= f.input :comment, input_html: { value: nil } %>
                        <%= f.button :submit, class: "btn btn-gradient-oval" %>
                        <% end %>
                      </div>
                    </div>
                  </div>
                </div>
              </li>
            <% end %>
          </ul>
        </div>
        <% end %>
      </div>
    </div>
  </div>

<%# Accordion-Collabolators %>
    <div class="accordion-item">
    <h2 class="accordion-header" id="panelsStayOpen-headingSix">
      <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseSix" aria-expanded="false" aria-controls="panelsStayOpen-collapseSix">
        <strong>Collaborators</strong>
      </button>
    </h2>
    <div id="panelsStayOpen-collapseSix" class="accordion-collapse collapse" aria-labelledby="panelsStayOpen-headingSeven">
      <div class="accordion-body">
        <ul>
          <% @event.collaborators.each do |collaborator| %>
            <li><%= collaborator.user.first_name + " " + collaborator.user.last_name %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>

</div>
</div>
